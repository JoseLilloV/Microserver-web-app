spring:
  application:
    name: Reloj-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/as2_reloj
    username: postgres
    password: A1b2c3Z0
  jpa:
    hibernate:
      ddl-auto: update